# NetCord API Tests

Package-level unit tests live next to the code they cover under `internal`.

Use this directory for future integration and end-to-end API tests that need PostgreSQL, Redis, MinIO, or a running HTTP server.
